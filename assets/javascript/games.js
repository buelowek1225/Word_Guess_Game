// list of unique geographic regions or locations
let geolocations = [
    "machu picchu", 
    "rocky mountains", 
    "sahara desert", 
    "mauna loa", 
    "angel water falls"
]
// console.log(geolocations);

//randomly pick a value from my array
var rand = geolocations[Math.floor(Math.random() * geolocations.length)];
// console.log(rand);

// key listener, waits for key to be pressed and then reports the key name
document.addEventListener("keydown",function(e){
});
console.log(e.key);

